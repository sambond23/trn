# TRN — Trusted Relay Network

Децентрализованная сеть приватности с маршрутизацией, шифрованием, обфускацией и фрагментацией.

## Сборка

```bash
make          # сборка обоих бинарников
make build-node   # только node
make build-client # только client